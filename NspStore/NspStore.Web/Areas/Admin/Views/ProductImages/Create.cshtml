@model NspStore.Domain.Entities.ProductImage
@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
    ViewData["Title"] = "Добавить изображение";
    var productId = ViewBag.ProductId as int? ?? 0;
}

<section class="section admin-product-images">
    <div class="container">
        <h1 class="h1">Добавить изображение</h1>

        <form asp-action="Create" method="post" class="form">
            <input type="hidden" asp-for="ProductId" value="@productId" />

            <div class="form__row">
                <label asp-for="Url" class="form__label">URL изображения</label>
                <input asp-for="Url" class="input" />
                <span asp-validation-for="Url" class="input__error"></span>
            </div>

            <div class="form__row">
                <label asp-for="SortOrder" class="form__label">Порядок</label>
                <input asp-for="SortOrder" class="input" type="number" />
                <span asp-validation-for="SortOrder" class="input__error"></span>
            </div>

            <div class="form__actions">
                <button type="submit" class="btn btn--primary">Сохранить</button>
                <a class="btn btn--secondary"
                   asp-action="Index"
                   asp-route-productId="@productId">
                    Отмена
                </a>
            </div>
        </form>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
